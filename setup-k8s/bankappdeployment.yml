apiVersion: apps/v1
kind: Deployment
metadata:
  name: bankapp-deployment
  labels:
    app: bankapp
  namespace: bankapp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: bankapp
  template:
    metadata:
      labels:
        app: bankapp
    spec:
      containers:
      - name: bankapp-cont
        imagePullPolicy: IfNotPresent
        image: sandippatil6696/ai-bankapp:v1
        ports:
        - containerPort: 8080

        envFrom:
        - configMapRef:
            name: bankapp-configmap

        - secretRef:
            name: bankapp-secrets
        
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"